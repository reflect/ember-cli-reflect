{{reflect-view
  filters=filters
  token=token
  project=project
  events=events
  dates=dates
  colors=colors
  formatters=formatters
  overrides=overrides
  timezone=timezone
  view=viewName
  interactions=interactions
}}
